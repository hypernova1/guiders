<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guiders.web.member.dao.MemberDAO">
  <insert id="insertMember">
    INSERT INTO member (email, password, mname, gender, phone, photo, ctno, regdate)
                VALUES (#{email}, #{password}, #{manme}, #{gender}, #{phone},
                       #{photo}, #{photo}, #{ctno}, now())
  </insert>
  <insert id="insertAuth">
    INSERT INTO member_auth (#{email}, 'roll_guider')
    
  </insert>
</mapper>